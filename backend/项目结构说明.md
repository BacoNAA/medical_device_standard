# 后端项目结构说明

## 项目信息

- **项目名称：** 医疗器械标准数据库与应用系统
- **技术栈：** Spring Boot 3.5.7 + MyBatis-Plus 3.5.9 + MySQL 8.0
- **Java版本：** 17
- **构建工具：** Maven 3.6+

---

## 目录结构

```
backend/
├── src/main/java/com/medical/standard/
│   ├── MedicalStandardDatabaseApplication.java  # 主启动类
│   ├── common/                                   # 公共类
│   │   └── Result.java                          # 统一响应结果类
│   ├── config/                                   # 配置类
│   │   ├── MyBatisPlusConfig.java               # MyBatis-Plus配置（PascalCase自动填充）
│   │   ├── CorsConfig.java                      # 跨域配置
│   │   └── Knife4jConfig.java                   # API文档配置
│   ├── controller/                               # 控制器层（待开发）
│   ├── service/                                  # 服务接口层（待开发）
│   │   └── impl/                                # 服务实现层（待开发）
│   ├── mapper/                                   # 数据访问层（已完成 ✅）
│   │   ├── UserMapper.java                      # 用户表Mapper
│   │   ├── StandardMapper.java                  # 标准信息表Mapper
│   │   ├── RequirementItemMapper.java           # 检验项目表Mapper
│   │   ├── TermDefinitionMapper.java            # 术语定义表Mapper
│   │   ├── ClauseMapper.java                    # 条款信息表Mapper
│   │   ├── MethodMapper.java                    # 检验方法表Mapper
│   │   ├── AppendixMapper.java                  # 附录表Mapper
│   │   ├── EquipmentMapper.java                 # 设备信息表Mapper
│   │   ├── ItemEquipmentMapper.java             # 项目设备关联表Mapper
│   │   ├── EquipmentKeywordMapper.java          # 设备关键词表Mapper
│   │   ├── ErrorReportMapper.java               # 错误报告表Mapper
│   │   ├── ExportTemplateMapper.java            # 自定义输出模板表Mapper
│   │   ├── TemplateFieldMapper.java             # 模板字段配置表Mapper
│   │   ├── PTRDocumentMapper.java               # 产品技术要求表Mapper
│   │   └── OperationLogMapper.java              # 操作日志表Mapper
│   ├── entity/                                   # 实体类（已完成 ✅）
│   │   ├── BaseEntity.java                      # 基础实体类（PascalCase字段）
│   │   ├── User.java                            # 用户表实体
│   │   ├── Standard.java                        # 标准信息表实体
│   │   ├── RequirementItem.java                 # 检验项目表实体
│   │   ├── TermDefinition.java                  # 术语定义表实体
│   │   ├── Clause.java                          # 条款信息表实体
│   │   ├── Method.java                          # 检验方法表实体
│   │   ├── Appendix.java                        # 附录表实体
│   │   ├── Equipment.java                       # 设备信息表实体
│   │   ├── ItemEquipment.java                   # 项目设备关联表实体
│   │   ├── EquipmentKeyword.java                # 设备关键词表实体
│   │   ├── ErrorReport.java                     # 错误报告表实体
│   │   ├── ExportTemplate.java                  # 自定义输出模板表实体
│   │   ├── TemplateField.java                   # 模板字段配置表实体
│   │   ├── PTRDocument.java                     # 产品技术要求表实体
│   │   └── OperationLog.java                    # 操作日志表实体
│   ├── dto/                                      # 数据传输对象（待开发）
│   ├── vo/                                       # 视图对象（待开发）
│   ├── exception/                                # 异常处理
│   │   ├── BusinessException.java               # 业务异常类
│   │   └── GlobalExceptionHandler.java          # 全局异常处理器
│   └── util/                                     # 工具类
│       └── JwtUtil.java                         # JWT工具类
├── src/main/resources/
│   ├── application.yaml                          # 主配置文件
│   ├── application-dev.yaml                      # 开发环境配置
│   ├── application-prod.yaml                     # 生产环境配置
│   ├── sql/                                      # SQL脚本（已完成 ✅）
│   │   ├── 01_create_database.sql               # 创建数据库
│   │   ├── 02_create_tables.sql                 # 创建15个数据表
│   │   ├── 03_insert_init_data.sql              # 插入初始数据
│   │   └── README.md                            # SQL脚本说明
│   ├── mapper/                                   # MyBatis XML映射文件（待开发）
│   ├── static/                                   # 静态资源
│   └── templates/                                # 模板文件
├── src/test/java/com/medical/standard/
│   ├── MedicalStandardDatabaseApplicationTests.java  # 主测试类
│   └── DatabaseConnectionTest.java              # 数据库连接测试（已完成 ✅）
├── pom.xml                                       # Maven配置文件
├── HELP.md                                       # Spring Boot帮助文档
├── 项目结构说明.md                               # 本文档
├── 数据库初始化说明.md                           # 数据库初始化步骤
├── VSCode数据库连接指南.md                       # VSCode MySQL扩展使用指南
└── 数据库连接验证指南.md                         # 数据库连接测试和故障排查
```

---

## 核心组件说明

### 1. 统一响应类（Result）

**位置：** `com.medical.standard.common.Result`

**功能：** 统一封装所有API的响应结果

**使用示例：**
```java
// 成功响应
return Result.success(data);

// 失败响应
return Result.error("操作失败");
```

---

### 2. 基础实体类（BaseEntity）

**位置：** `com.medical.standard.entity.BaseEntity`

**功能：** 部分数据库实体类的父类，包含公共字段（严格按照设计文档 v2.0 - PascalCase命名规范）

**包含字段：**
- `CreatedBy` - 创建人ID（自动填充）
- `CreatedTime` - 创建时间（自动填充）
- `UpdatedBy` - 更新人ID（自动填充）
- `UpdatedTime` - 更新时间（自动填充）

**注意：**
- 不包含主键字段，因为每个表的主键名称不同（如UserID、ItemID、StandardNo等）
- 不包含逻辑删除字段（设计文档未要求）
- 字段名使用PascalCase，与数据库字段名完全一致

---

### 3. 全局异常处理器（GlobalExceptionHandler）

**位置：** `com.medical.standard.exception.GlobalExceptionHandler`

**功能：** 统一处理所有异常，返回标准化的错误响应

**处理的异常类型：**
- `BusinessException` - 业务异常
- `MethodArgumentNotValidException` - 参数校验异常
- `BindException` - 参数绑定异常
- `ConstraintViolationException` - 约束违反异常
- `IllegalArgumentException` - 非法参数异常
- `NullPointerException` - 空指针异常
- `Exception` - 其他未知异常

---

### 4. JWT工具类（JwtUtil）

**位置：** `com.medical.standard.util.JwtUtil`

**功能：** JWT Token的生成、解析和验证

**主要方法：**
- `generateToken(userId, username, role)` - 生成Token
- `parseToken(token)` - 解析Token
- `validateToken(token)` - 验证Token
- `getUserIdFromToken(token)` - 获取用户ID
- `getUsernameFromToken(token)` - 获取用户名
- `getRoleFromToken(token)` - 获取角色
- `refreshToken(token)` - 刷新Token

---

### 5. MyBatis-Plus配置（MyBatisPlusConfig）

**位置：** `com.medical.standard.config.MyBatisPlusConfig`

**功能：** 配置MyBatis-Plus的自动填充功能

**自动填充规则：**
- 插入时：自动填充 `createTime` 和 `updateTime`
- 更新时：自动填充 `updateTime`

---

### 6. 跨域配置（CorsConfig）

**位置：** `com.medical.standard.config.CorsConfig`

**功能：** 配置跨域访问策略，允许前端调用后端API

**配置说明：**
- 允许所有域名跨域（开发环境）
- 允许所有请求头和请求方法
- 允许携带凭证（Cookie）
- 预检请求有效期：3600秒

---

### 7. Knife4j配置（Knife4jConfig）

**位置：** `com.medical.standard.config.Knife4jConfig`

**功能：** 配置API文档（Swagger UI）

**访问地址：** `http://localhost:8080/doc.html`

**配置说明：**
- 支持JWT Token认证
- 中文界面
- 包含项目基本信息

---

## 配置文件说明

### application.yaml（主配置）

- 激活开发环境配置（`spring.profiles.active: dev`）
- 文件上传大小限制（50MB/100MB）
- Jackson JSON序列化配置
- MyBatis-Plus配置：
  - 日志实现：`StdOutImpl`（控制台输出SQL）
  - **下划线转驼峰：`false`**（⚠️ 重要：因为使用PascalCase命名）
  - **逻辑删除：已禁用**（设计文档未要求）
- Knife4j配置：启用API文档
- 日志配置

### application-dev.yaml（开发环境）

- 服务器端口：8080
- 数据库连接：`localhost:3306/medical_device_standards`（严格按照设计文档）
- 数据库用户名：root
- 数据库密码：123456（⚠️ 需要修改为实际密码）
- JWT密钥和过期时间
- 文件存储路径

### application-prod.yaml（生产环境）

- 使用环境变量配置敏感信息
- 生产环境连接池配置
- 生产环境日志配置

---

## 开发进度

### ✅ 已完成

#### 1. 项目基础架构
- [x] Spring Boot 3.5.7 项目初始化
- [x] Maven依赖配置（pom.xml）
- [x] 基础配置类（MyBatisPlusConfig、CorsConfig、Knife4jConfig）
- [x] 公共类（Result、BusinessException、GlobalExceptionHandler、JwtUtil）
- [x] BaseEntity基础实体类（PascalCase字段）

#### 2. 数据库设计与初始化
- [x] 数据库设计文档 v2.0（15个表，PascalCase命名）
- [x] SQL脚本创建（01_create_database.sql、02_create_tables.sql、03_insert_init_data.sql）
- [x] 数据库初始化说明文档
- [x] VSCode数据库连接指南
- [x] 数据库连接验证指南

#### 3. 实体类开发（15个实体类）
- [x] User（用户表）
- [x] Standard（标准信息表）
- [x] RequirementItem（检验项目表）
- [x] TermDefinition（术语定义表）
- [x] Clause（条款信息表）
- [x] Method（检验方法表）
- [x] Appendix（附录表）
- [x] Equipment（设备信息表）
- [x] ItemEquipment（项目设备关联表）
- [x] EquipmentKeyword（设备关键词表）
- [x] ErrorReport（错误报告表）
- [x] ExportTemplate（自定义输出模板表）
- [x] TemplateField（模板字段配置表）
- [x] PTRDocument（产品技术要求表）
- [x] OperationLog（操作日志表）

**特性：** 所有实体类严格按照设计文档 v2.0 使用PascalCase命名规范

#### 4. Mapper接口开发（15个Mapper接口）
- [x] UserMapper
- [x] StandardMapper
- [x] RequirementItemMapper
- [x] TermDefinitionMapper
- [x] ClauseMapper
- [x] MethodMapper
- [x] AppendixMapper
- [x] EquipmentMapper
- [x] ItemEquipmentMapper
- [x] EquipmentKeywordMapper
- [x] ErrorReportMapper
- [x] ExportTemplateMapper
- [x] TemplateFieldMapper
- [x] PTRDocumentMapper
- [x] OperationLogMapper

#### 5. 测试代码
- [x] DatabaseConnectionTest（数据库连接测试，5个测试用例）

---

### 📋 待开发

#### 1. 功能模块开发（按WBS顺序）
- [ ] 用户管理模块（Week 3）
  - [ ] UserService、UserController
  - [ ] 登录、注册、用户CRUD
- [ ] 标准数据管理模块（Week 4-5）
  - [ ] StandardService、StandardController
  - [ ] 标准浏览、搜索、导入
- [ ] 设备管理模块（Week 6）
  - [ ] EquipmentService、EquipmentController
  - [ ] 设备目录管理、关键词识别
- [ ] 数据导出模块（Week 7）
  - [ ] ExportService、ExportController
  - [ ] 3种预定义表格、自定义模板
- [ ] PTR编辑器模块（Week 8）
  - [ ] PTRDocumentService、PTRDocumentController
  - [ ] 富文本编辑、智能内容建议
- [ ] 错误报告模块（Week 8）
  - [ ] ErrorReportService、ErrorReportController
  - [ ] 错误跟踪、修正工作流

#### 2. DTO和VO开发
- [ ] 用户相关DTO/VO
- [ ] 标准相关DTO/VO
- [ ] 设备相关DTO/VO
- [ ] 导出相关DTO/VO
- [ ] PTR相关DTO/VO
- [ ] 错误报告相关DTO/VO

---

## 编译和运行

### 编译项目
```bash
cd backend
mvn clean compile
```

### 运行项目
```bash
cd backend
mvn spring-boot:run
```

### 访问API文档
```
http://localhost:8080/doc.html
```

---

## 重要注意事项

### ⚠️ 必须修改的配置

1. **数据库密码**：`application-dev.yaml` 中的数据库密码需要修改为实际密码
2. **生产环境配置**：生产环境需要配置环境变量 `DB_PASSWORD` 和 `JWT_SECRET`
3. **跨域配置**：生产环境建议指定具体的前端域名，不要使用 `*`

### ✅ 关键设计决策

1. **PascalCase命名规范**：
   - 数据库表名和字段名使用PascalCase（如 `User`、`UserID`、`CreatedTime`）
   - 实体类字段名与数据库字段名完全一致
   - **必须关闭MyBatis-Plus的下划线转驼峰**（`map-underscore-to-camel-case: false`）

2. **表特定主键**：
   - 每个表使用表特定的主键名（如 `UserID`、`ItemID`、`StandardNo`）
   - 不使用通用的 `id` 字段
   - BaseEntity不包含主键字段

3. **BaseEntity设计**：
   - 只包含 `CreatedBy`、`CreatedTime`、`UpdatedBy`、`UpdatedTime` 四个字段
   - 不包含主键字段（每个表主键名不同）
   - 不包含逻辑删除字段（设计文档未要求）
   - 只有部分表继承BaseEntity（根据SQL表结构决定）

4. **自动填充机制**：
   - `CreatedTime` 和 `UpdatedTime` 由MyBatis-Plus自动填充
   - `CreatedBy` 和 `UpdatedBy` 需要在Service层手动设置（从当前登录用户获取）

5. **Lombok支持**：
   - 已配置Lombok注解处理器
   - 支持 `@Data`、`@Slf4j`、`@Builder` 等注解

### 📚 相关文档

- **数据库初始化**：`backend/数据库初始化说明.md`
- **VSCode连接指南**：`backend/VSCode数据库连接指南.md`
- **连接验证指南**：`backend/数据库连接验证指南.md`
- **设计文档**：`project_document/数据库设计文档-医疗器械标准数据库与应用系统_v2.0.md`

---

**创建时间：** 2025-11-14  
**版本：** v1.0

