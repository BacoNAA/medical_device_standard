# 开发环境配置与项目初始化指南

**项目名称：** 医疗器械标准数据库与应用系统
**开发模式：** 前后端分离
**Spring Boot版本：** 3.5.7
**创建日期：** 2025-11-12
**编写人员：** 高级系统架构师

---

## 📋 目录

1. [开发环境准备](#1-开发环境准备)
2. [后端项目初始化](#2-后端项目初始化)
3. [前端项目初始化](#3-前端项目初始化)
4. [数据库初始化](#4-数据库初始化)
5. [开发工具配置](#5-开发工具配置)
6. [项目启动验证](#6-项目启动验证)
7. [下一步开发计划](#7-下一步开发计划)

---

## 1. 开发环境准备

### 1.1 必需软件清单

#### 后端开发环境

| 软件 | 版本要求 | 下载地址 | 用途 |
|------|---------|---------|------|
| **JDK** | **17 或 21** ⚠️ | https://adoptium.net/ | Java运行环境（Spring Boot 3.x必须） |
| **Maven** | 3.6+ | https://maven.apache.org/download.cgi | 项目构建工具 |
| **IntelliJ IDEA** | 2023+ (Community版即可) | https://www.jetbrains.com/idea/download/ | Java IDE |
| **MySQL** | 8.0+ | https://dev.mysql.com/downloads/mysql/ | 数据库 |

> ⚠️ **重要提示：** Spring Boot 3.5.7 **必须使用 Java 17 或更高版本**，不支持Java 8/11！

#### 前端开发环境

| 软件 | 版本要求 | 下载地址 | 用途 |
|------|---------|---------|------|
| **Node.js** | 16+ (推荐18 LTS) | https://nodejs.org/ | JavaScript运行环境 |
| **npm** | 8+ (随Node.js安装) | - | 包管理器 |
| **VS Code** | 最新版 | https://code.visualstudio.com/ | 前端IDE |

#### 数据库管理工具

| 软件 | 版本要求 | 下载地址 | 用途 |
|------|---------|---------|------|
| **Navicat** 或 **MySQL Workbench** | 最新版 | https://www.navicat.com.cn/ | 数据库可视化管理 |

---

### 1.2 环境验证

安装完成后，打开命令行验证：

```bash
# 验证Java（⚠️ 必须是17或21）
java -version
# 应显示：java version "17.x.x" 或 "21.x.x"
# ❌ 如果显示 "1.8.0" 或 "11.x.x"，需要升级到Java 17或21

# 验证Maven
mvn -version
# 应显示：Apache Maven 3.6.x 或更高

# 验证Node.js
node -v
# 应显示：v16.x.x 或更高

# 验证npm
npm -v
# 应显示：8.x.x 或更高

# 验证MySQL
mysql --version
# 应显示：mysql  Ver 8.0.x
```

> ⚠️ **如果Java版本不是17或21，请先安装：**
> - 推荐使用 Eclipse Temurin (OpenJDK): https://adoptium.net/
> - 或 Oracle JDK 17: https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html

---

## 2. 后端项目初始化（Spring Boot 3.5.7）

### 2.1 使用Spring Initializr创建项目

**方式1：在线生成（推荐）**

1. 访问：https://start.spring.io/

2. 配置项目参数：
   - **Project:** Maven ✅
   - **Language:** Java ✅
   - **Spring Boot:** **3.5.7** ✅

   - **Project Metadata:**
     - Group: `com.medical` ✅
     - Artifact: `standard-database` ✅
     - Name: `medical-standard-database` ✅
     - Description: `医疗器械标准数据库与应用系统` ✅
     - Package name: `com.medical.standard` ✅
     - Packaging: **Jar** ✅（选择Jar，不要选War）
     - Java: **17** ✅（或21，⚠️ 不能选8/11）

   - **Configuration:**
     - 选择 **YAML** ✅（不要选Properties）

3. 添加依赖（Dependencies）- 点击 **ADD DEPENDENCIES** 按钮：
   - **Spring Web** - 构建RESTful API
   - **Spring Boot DevTools** - 热部署
   - **Lombok** - 简化代码
   - **MySQL Driver** - MySQL数据库驱动
   - **MyBatis Framework** - ORM框架

   **共5个依赖**

4. 点击 **GENERATE** 下载项目压缩包

5. 解压到工作目录：
   ```bash
   # 解压到项目根目录
   unzip medical-standard-database.zip -d d:/软件项目管理/backend
   ```

**方式2：使用IntelliJ IDEA创建**

1. 打开IntelliJ IDEA
2. File → New → Project → Spring Initializr
3. 按照上述参数配置（确保Spring Boot版本选择3.5.7，Java选择17或21）
4. 选择保存路径：`d:/软件项目管理/backend`

---

### 2.2 后端项目结构规划

```
backend/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── medical/
│   │   │           └── standard/
│   │   │               ├── MedicalStandardApplication.java  # 启动类
│   │   │               ├── config/                          # 配置类
│   │   │               │   ├── CorsConfig.java             # 跨域配置
│   │   │               │   ├── MyBatisPlusConfig.java      # MyBatis-Plus配置
│   │   │               │   └── SecurityConfig.java         # 安全配置
│   │   │               ├── controller/                      # 控制器层
│   │   │               │   ├── AuthController.java         # 登录认证
│   │   │               │   ├── StandardController.java     # 标准管理
│   │   │               │   ├── EquipmentController.java    # 设备管理
│   │   │               │   ├── ExportController.java       # 数据导出
│   │   │               │   └── PTRController.java          # PTR编辑
│   │   │               ├── service/                         # 服务层
│   │   │               │   ├── IUserService.java
│   │   │               │   ├── IStandardService.java
│   │   │               │   ├── IEquipmentService.java
│   │   │               │   └── impl/                        # 实现类
│   │   │               ├── mapper/                          # 数据访问层
│   │   │               │   ├── UserMapper.java
│   │   │               │   ├── StandardMapper.java
│   │   │               │   └── EquipmentMapper.java
│   │   │               ├── entity/                          # 实体类
│   │   │               │   ├── User.java
│   │   │               │   ├── Standard.java
│   │   │               │   ├── RequirementItem.java
│   │   │               │   └── Equipment.java
│   │   │               ├── dto/                             # 数据传输对象
│   │   │               │   ├── LoginRequest.java
│   │   │               │   ├── LoginResponse.java
│   │   │               │   └── ImportPreviewResponse.java
│   │   │               ├── vo/                              # 视图对象
│   │   │               ├── common/                          # 公共类
│   │   │               │   ├── Result.java                 # 统一响应
│   │   │               │   ├── ResultCode.java             # 响应码
│   │   │               │   └── exception/                   # 异常处理
│   │   │               └── util/                            # 工具类
│   │   │                   ├── JwtUtil.java                # JWT工具
│   │   │                   ├── ExcelUtil.java              # Excel工具
│   │   │                   └── WordUtil.java               # Word工具
│   │   └── resources/
│   │       ├── application.yml                              # 主配置文件
│   │       ├── application-dev.yml                          # 开发环境配置
│   │       ├── application-prod.yml                         # 生产环境配置
│   │       ├── mapper/                                      # MyBatis XML
│   │       │   ├── UserMapper.xml
│   │       │   └── StandardMapper.xml
│   │       └── db/
│   │           └── migration/                               # 数据库脚本
│   │               └── V1__init_schema.sql
│   └── test/                                                # 测试代码
│       └── java/
│           └── com/
│               └── medical/
│                   └── standard/
├── pom.xml                                                  # Maven配置
└── README.md
```

---

## 3. 前端项目初始化

### 3.1 创建Vue 3项目

```bash
# 进入工作目录
cd d:/软件项目管理

# 使用Vite创建Vue 3项目
npm create vite@latest frontend -- --template vue

# 进入项目目录
cd frontend

# 安装依赖
npm install

# 安装Element Plus
npm install element-plus

# 安装图标库
npm install @element-plus/icons-vue

# 安装Axios
npm install axios

# 安装Vue Router
npm install vue-router@4

# 安装Pinia
npm install pinia

# 安装富文本编辑器（可选）
npm install @vueup/vue-quill
```

---

### 3.2 前端项目结构规划

```
frontend/
├── public/                          # 静态资源
│   └── favicon.ico
├── src/
│   ├── assets/                      # 资源文件
│   │   ├── styles/
│   │   │   ├── global.css          # 全局样式
│   │   │   └── variables.css       # CSS变量
│   │   └── images/
│   ├── components/                  # 公共组件
│   │   ├── Layout/
│   │   │   ├── Header.vue          # 顶部导航
│   │   │   ├── Sidebar.vue         # 侧边菜单
│   │   │   └── MainLayout.vue      # 主布局
│   │   └── Common/
│   │       ├── Pagination.vue      # 分页组件
│   │       └── SearchBar.vue       # 搜索栏
│   ├── views/                       # 页面组件
│   │   ├── Login.vue               # 登录页
│   │   ├── Dashboard.vue           # 工作台
│   │   ├── Standards/              # 标准管理
│   │   │   ├── List.vue           # 标准列表
│   │   │   ├── Detail.vue         # 标准详情
│   │   │   └── Import.vue         # 标准导入
│   │   ├── Equipment/              # 设备管理
│   │   │   └── Manage.vue
│   │   ├── Export/                 # 数据导出
│   │   │   ├── Table1.vue
│   │   │   ├── Table2.vue
│   │   │   ├── Table3.vue
│   │   │   └── Custom.vue
│   │   ├── PTREditor/              # PTR编辑器
│   │   │   └── index.vue
│   │   ├── ErrorReports/           # 错误报告
│   │   │   └── index.vue
│   │   └── System/                 # 系统管理
│   │       ├── Users.vue
│   │       ├── Logs.vue
│   │       └── Settings.vue
│   ├── router/                      # 路由配置
│   │   └── index.js
│   ├── stores/                      # 状态管理
│   │   ├── user.js                 # 用户状态
│   │   └── standards.js            # 标准数据状态
│   ├── api/                         # API接口
│   │   ├── auth.js                 # 认证接口
│   │   ├── standard.js             # 标准接口
│   │   ├── equipment.js            # 设备接口
│   │   └── export.js               # 导出接口
│   ├── utils/                       # 工具函数
│   │   ├── request.js              # Axios封装
│   │   ├── auth.js                 # 认证工具
│   │   └── constants.js            # 常量定义
│   ├── App.vue                      # 根组件
│   └── main.js                      # 入口文件
├── .env.development                 # 开发环境变量
├── .env.production                  # 生产环境变量
├── vite.config.js                   # Vite配置
├── package.json
└── README.md
```

---

## 4. 数据库初始化

### 4.1 创建数据库

```sql
-- 连接MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE medical_standard_db 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- 创建用户（可选）
CREATE USER 'medical_user'@'localhost' IDENTIFIED BY 'Medical@2024';

-- 授权
GRANT ALL PRIVILEGES ON medical_standard_db.* TO 'medical_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 使用数据库
USE medical_standard_db;
```

### 4.2 执行建表脚本

参考《数据库设计文档_v1.0.md》附录A的建表脚本，创建以下核心表：

1. `User` - 用户表
2. `Standard` - 标准表
3. `RequirementItem` - 检验项目表
4. `Equipment` - 设备表
5. `ItemEquipment` - 项目设备关联表
6. `EquipmentKeyword` - 设备关键词表
7. `ErrorReport` - 错误报告表
8. `OperationLog` - 操作日志表

**执行方式：**
```bash
# 方式1：命令行执行
mysql -u root -p medical_standard_db < backend/src/main/resources/db/migration/V1__init_schema.sql

# 方式2：使用Navicat
# 打开Navicat → 连接数据库 → 运行SQL文件
```

---

## 5. 开发工具配置

### 5.1 IntelliJ IDEA配置（后端）

**推荐插件：**
1. **Lombok** - 简化Java代码
2. **MyBatisX** - MyBatis增强
3. **RestfulToolkit** - API测试
4. **Maven Helper** - 依赖管理

**配置步骤：**
1. File → Settings → Plugins → 搜索并安装上述插件
2. File → Settings → Build, Execution, Deployment → Compiler → 勾选 "Build project automatically"
3. File → Settings → Editor → File Encodings → 设置为 UTF-8

---

### 5.2 VS Code配置（前端）

**推荐插件：**
1. **Volar** - Vue 3官方插件
2. **ESLint** - 代码规范检查
3. **Prettier** - 代码格式化
4. **Auto Rename Tag** - 自动重命名标签
5. **Path Intellisense** - 路径智能提示

**配置步骤：**
1. 打开VS Code
2. Extensions → 搜索并安装上述插件
3. File → Preferences → Settings → 搜索 "format on save" → 勾选

---

## 6. 项目启动验证

### 6.1 启动后端项目

```bash
# 进入后端目录
cd d:/软件项目管理/backend

# 启动Spring Boot应用
mvn spring-boot:run

# 或者在IntelliJ IDEA中
# 右键 MedicalStandardApplication.java → Run
```

**验证：**
- 浏览器访问：http://localhost:8080
- 应看到Spring Boot默认页面或自定义欢迎页

---

### 6.2 启动前端项目

```bash
# 进入前端目录
cd d:/软件项目管理/frontend

# 启动开发服务器
npm run dev
```

**验证：**
- 浏览器访问：http://localhost:5173
- 应看到Vue 3默认欢迎页

---

## 7. 下一步开发计划

### 阶段1：基础框架搭建（第1-2周）

**后端任务：**
- [ ] 配置`application.yml`（数据库连接、端口等）
- [ ] 配置MyBatis-Plus（分页插件、逻辑删除）
- [ ] 配置跨域（CorsConfig）
- [ ] 实现统一响应类（Result）
- [ ] 实现全局异常处理
- [ ] 实现JWT工具类

**前端任务：**
- [ ] 配置Axios（请求拦截器、响应拦截器）
- [ ] 配置Vue Router（路由守卫）
- [ ] 配置Pinia（用户状态管理）
- [ ] 实现主布局（Header + Sidebar）
- [ ] 实现登录页面
- [ ] 实现路由权限控制

---

### 阶段2：核心功能开发（第3-8周）

按照WBS_v3.0的6个功能模块依次开发：

1. **用户管理模块**（第3周）
   - 用户登录
   - 角色权限控制

2. **标准数据管理模块**（第4-5周）
   - 标准Excel导入（最复杂）
   - 标准列表和详情
   - 标准检索

3. **设备管理模块**（第6周）
   - 设备信息维护
   - 设备关键词库

4. **数据导出模块**（第7周）
   - 表1/2/3导出
   - 自定义导出

5. **PTR编辑模块**（第8周）
   - 富文本编辑器
   - Word导出

6. **错误报告模块**（第8周）
   - 错误提交和处理

---

### 阶段3：测试与优化（第9-10周）

- [ ] 单元测试
- [ ] 集成测试
- [ ] 性能优化
- [ ] 安全加固

---

---

## 附录A：后端配置文件模板（Spring Boot 3.5.7）

### A.1 pom.xml（Maven依赖配置）

> ⚠️ **重要：** 这是Spring Boot 3.5.7版本的配置，与2.x版本有重要区别！

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.7</version>
        <relativePath/>
    </parent>

    <groupId>com.medical</groupId>
    <artifactId>standard-database</artifactId>
    <version>1.0.0</version>
    <name>medical-standard-database</name>
    <description>医疗器械标准数据库与应用系统</description>

    <properties>
        <java.version>17</java.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <easyexcel.version>4.0.3</easyexcel.version>
        <poi.version>5.3.0</poi.version>
        <jjwt.version>0.12.6</jjwt.version>
        <hutool.version>5.8.34</hutool.version>
        <knife4j.version>4.5.0</knife4j.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- MyBatis-Plus（⚠️ 注意：Spring Boot 3.x使用不同的starter） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>

        <!-- MySQL驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- EasyExcel -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>easyexcel</artifactId>
            <version>${easyexcel.version}</version>
        </dependency>

        <!-- Apache POI (Word生成) -->
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Hutool工具库 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
            <version>${hutool.version}</version>
        </dependency>

        <!-- Knife4j（Swagger增强，⚠️ 注意：Spring Boot 3.x使用不同的starter） -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>${knife4j.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot DevTools -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
```

**关键版本变化说明：**

| 组件 | Spring Boot 2.7.x | Spring Boot 3.5.7 | 说明 |
|------|------------------|------------------|------|
| **Java版本** | 8, 11, 17 | **17, 21** | ⚠️ 必须17+ |
| **MyBatis-Plus Starter** | `mybatis-plus-boot-starter` | `mybatis-plus-spring-boot3-starter` | 包名变化 |
| **Knife4j Starter** | `knife4j-spring-boot-starter` | `knife4j-openapi3-jakarta-spring-boot-starter` | 包名变化 |
| **MyBatis-Plus版本** | 3.5.5 | **3.5.9** | 版本升级 |
| **EasyExcel版本** | 3.3.3 | **4.0.3** | 版本升级 |
| **POI版本** | 5.2.5 | **5.3.0** | 版本升级 |
| **JWT版本** | 0.11.5 | **0.12.6** | 版本升级 |

---

### A.2 application.yml（主配置文件）

```yaml
spring:
  profiles:
    active: dev  # 激活开发环境配置

  application:
    name: medical-standard-database

  servlet:
    multipart:
      max-file-size: 50MB      # 单个文件最大50MB
      max-request-size: 100MB  # 请求最大100MB

  # Jackson配置（JSON序列化）
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss
    default-property-inclusion: non_null

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 打印SQL日志
    map-underscore-to-camel-case: true  # 下划线转驼峰
  global-config:
    db-config:
      logic-delete-field: deleted  # 逻辑删除字段
      logic-delete-value: 1        # 删除值
      logic-not-delete-value: 0    # 未删除值
  mapper-locations: classpath*:/mapper/**/*.xml

# Knife4j配置（API文档）
knife4j:
  enable: true
  setting:
    language: zh_cn

# 日志配置
logging:
  level:
    com.medical.standard: debug
    com.medical.standard.mapper: debug
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n"
```

---

### A.3 application-dev.yml（开发环境配置）

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/medical_standard_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456  # ⚠️ 修改为您的MySQL密码
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

# JWT配置
jwt:
  secret: medical-standard-secret-key-2024-very-long-secret-for-jwt-token
  expiration: 86400000  # 24小时（毫秒）

# 文件存储路径
file:
  upload-path: D:/medical-standard-files/uploads/
  export-path: D:/medical-standard-files/exports/
```

---

### A.4 application-prod.yml（生产环境配置）

```yaml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/medical_standard_db?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: medical_user
    password: ${DB_PASSWORD}  # 从环境变量读取
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50
      connection-timeout: 30000

jwt:
  secret: ${JWT_SECRET}  # 从环境变量读取
  expiration: 86400000

file:
  upload-path: /data/medical-standard/uploads/
  export-path: /data/medical-standard/exports/

logging:
  level:
    com.medical.standard: info
```

---

## 附录B：前端配置文件模板

### B.1 vite.config.js（Vite配置）

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],

  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src')
    }
  },

  server: {
    port: 5173,
    host: '0.0.0.0',
    open: true,  // 自动打开浏览器

    // 代理配置（解决跨域）
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 后端地址
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },

  build: {
    outDir: 'dist',
    assetsDir: 'assets',
    sourcemap: false,

    // 生产环境移除console
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  }
})
```

---

### B.2 .env.development（开发环境变量）

```env
# 开发环境配置
VITE_APP_TITLE=医疗器械标准数据库系统
VITE_APP_BASE_API=/api
VITE_APP_PORT=5173
```

---

### B.3 .env.production（生产环境变量）

```env
# 生产环境配置
VITE_APP_TITLE=医疗器械标准数据库系统
VITE_APP_BASE_API=http://your-domain.com/api
```

---

### B.4 package.json（部分关键配置）

```json
{
  "name": "medical-standard-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "pinia": "^2.1.7",
    "element-plus": "^2.5.0",
    "@element-plus/icons-vue": "^2.3.1",
    "axios": "^1.6.2",
    "@vueup/vue-quill": "^1.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
```

---

## 附录C：快速启动脚本

### C.1 后端启动脚本（Windows）

创建文件：`backend/start-backend.bat`

```batch
@echo off
echo ========================================
echo 启动医疗器械标准数据库后端服务
echo ========================================
echo.

cd /d %~dp0

echo [1/3] 检查Java环境...
java -version
if errorlevel 1 (
    echo 错误：未检测到Java环境，请先安装JDK 8+
    pause
    exit /b 1
)

echo.
echo [2/3] 检查Maven环境...
mvn -version
if errorlevel 1 (
    echo 错误：未检测到Maven环境，请先安装Maven 3.6+
    pause
    exit /b 1
)

echo.
echo [3/3] 启动Spring Boot应用...
mvn spring-boot:run

pause
```

---

### C.2 前端启动脚本（Windows）

创建文件：`frontend/start-frontend.bat`

```batch
@echo off
echo ========================================
echo 启动医疗器械标准数据库前端服务
echo ========================================
echo.

cd /d %~dp0

echo [1/2] 检查Node.js环境...
node -v
if errorlevel 1 (
    echo 错误：未检测到Node.js环境，请先安装Node.js 16+
    pause
    exit /b 1
)

echo.
echo [2/2] 启动开发服务器...
npm run dev

pause
```

---

## 附录D：数据库初始化脚本

创建文件：`backend/src/main/resources/db/migration/V1__init_schema.sql`

```sql
-- 医疗器械标准数据库初始化脚本
-- 版本：v1.0
-- 日期：2025-11-12

-- 1. 用户表
CREATE TABLE `User` (
  `UserID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
  `Username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  `Password` VARCHAR(255) NOT NULL COMMENT '密码（加密）',
  `RealName` VARCHAR(100) COMMENT '真实姓名',
  `RoleType` TINYINT NOT NULL COMMENT '角色类型：1-数据输入员，2-系统管理员，3-业务人员，4-质量管理，5-设备管理，6-实验室人员',
  `Phone` VARCHAR(20) COMMENT '联系电话',
  `Email` VARCHAR(100) COMMENT '邮箱',
  `Status` TINYINT DEFAULT 1 COMMENT '状态：1-正常，0-禁用',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UpdatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `Deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
  INDEX idx_username (`Username`),
  INDEX idx_role_type (`RoleType`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 2. 标准表
CREATE TABLE `Standard` (
  `StandardID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '标准ID',
  `StandardNo` VARCHAR(50) NOT NULL UNIQUE COMMENT '标准编号（含年份，如GB 15810-2019）',
  `StandardName` VARCHAR(500) NOT NULL COMMENT '标准名称',
  `IssueDate` DATE COMMENT '发布日期',
  `ImplementDate` DATE COMMENT '实施日期',
  `Status` TINYINT DEFAULT 1 COMMENT '状态：1-现行，2-废止，3-即将实施',
  `CreatedBy` INT COMMENT '创建人ID',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UpdatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `Deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除',
  INDEX idx_standard_no (`StandardNo`),
  INDEX idx_standard_name (`StandardName`(100)),
  FOREIGN KEY (`CreatedBy`) REFERENCES `User`(`UserID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标准表';

-- 3. 检验项目表
CREATE TABLE `RequirementItem` (
  `ItemID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '项目ID',
  `StandardID` INT NOT NULL COMMENT '所属标准ID',
  `ClauseNo` VARCHAR(50) COMMENT '条款号（如5.1.1）',
  `ItemName` VARCHAR(500) NOT NULL COMMENT '项目名称',
  `RequirementContent` TEXT COMMENT '项目要求内容',
  `MethodType` TINYINT COMMENT '方法类型：1-无独立方法，2-引用附录，3-完整方法内容',
  `MethodContent` TEXT COMMENT '检验方法内容',
  `MethodReference` VARCHAR(200) COMMENT '方法引用（如"见附录A"）',
  `SortOrder` INT DEFAULT 0 COMMENT '排序号',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UpdatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `Deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除',
  INDEX idx_standard_id (`StandardID`),
  INDEX idx_item_name (`ItemName`(100)),
  FOREIGN KEY (`StandardID`) REFERENCES `Standard`(`StandardID`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='检验项目表';

-- 4. 设备表
CREATE TABLE `Equipment` (
  `EquipmentID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '设备ID',
  `EquipmentCode` VARCHAR(10) NOT NULL UNIQUE COMMENT '设备编号（如A001）',
  `EquipmentName` VARCHAR(200) NOT NULL COMMENT '设备名称',
  `Category` VARCHAR(50) COMMENT '设备类别',
  `Description` TEXT COMMENT '设备描述',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `UpdatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `Deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除',
  INDEX idx_equipment_code (`EquipmentCode`),
  INDEX idx_equipment_name (`EquipmentName`(50))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='设备表';

-- 5. 项目设备关联表
CREATE TABLE `ItemEquipment` (
  `ID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '关联ID',
  `ItemID` INT NOT NULL COMMENT '项目ID',
  `EquipmentID` INT NOT NULL COMMENT '设备ID',
  `SpecRequirement` VARCHAR(500) COMMENT '规格要求',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_item_id (`ItemID`),
  INDEX idx_equipment_id (`EquipmentID`),
  UNIQUE KEY uk_item_equipment (`ItemID`, `EquipmentID`),
  FOREIGN KEY (`ItemID`) REFERENCES `RequirementItem`(`ItemID`) ON DELETE CASCADE,
  FOREIGN KEY (`EquipmentID`) REFERENCES `Equipment`(`EquipmentID`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='项目设备关联表';

-- 6. 设备关键词表
CREATE TABLE `EquipmentKeyword` (
  `KeywordID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '关键词ID',
  `EquipmentID` INT NOT NULL COMMENT '设备ID',
  `Keyword` VARCHAR(100) NOT NULL COMMENT '关键词',
  `Priority` INT DEFAULT 0 COMMENT '优先级（数字越大优先级越高）',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_equipment_id (`EquipmentID`),
  INDEX idx_keyword (`Keyword`),
  FOREIGN KEY (`EquipmentID`) REFERENCES `Equipment`(`EquipmentID`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='设备关键词表';

-- 7. 错误报告表
CREATE TABLE `ErrorReport` (
  `ReportID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '报告ID',
  `StandardID` INT NOT NULL COMMENT '标准ID',
  `ItemID` INT COMMENT '项目ID（可选）',
  `ReporterID` INT NOT NULL COMMENT '报告人ID',
  `ErrorDescription` TEXT NOT NULL COMMENT '错误描述',
  `Status` TINYINT DEFAULT 1 COMMENT '状态：1-待处理，2-已处理，3-已驳回',
  `HandlerID` INT COMMENT '处理人ID',
  `HandleResult` TEXT COMMENT '处理结果',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `HandledTime` DATETIME COMMENT '处理时间',
  INDEX idx_standard_id (`StandardID`),
  INDEX idx_reporter_id (`ReporterID`),
  INDEX idx_status (`Status`),
  FOREIGN KEY (`StandardID`) REFERENCES `Standard`(`StandardID`),
  FOREIGN KEY (`ItemID`) REFERENCES `RequirementItem`(`ItemID`),
  FOREIGN KEY (`ReporterID`) REFERENCES `User`(`UserID`),
  FOREIGN KEY (`HandlerID`) REFERENCES `User`(`UserID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='错误报告表';

-- 8. 操作日志表
CREATE TABLE `OperationLog` (
  `LogID` INT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
  `UserID` INT COMMENT '操作用户ID',
  `OperationType` VARCHAR(50) NOT NULL COMMENT '操作类型（如：登录、导入标准、导出数据）',
  `OperationDetail` TEXT COMMENT '操作详情',
  `IPAddress` VARCHAR(50) COMMENT 'IP地址',
  `CreatedTime` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  INDEX idx_user_id (`UserID`),
  INDEX idx_operation_type (`OperationType`),
  INDEX idx_created_time (`CreatedTime`),
  FOREIGN KEY (`UserID`) REFERENCES `User`(`UserID`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';

-- 插入默认管理员账号（密码：admin123，需要在应用中加密）
INSERT INTO `User` (`Username`, `Password`, `RealName`, `RoleType`, `Status`)
VALUES ('sysadmin', 'admin123', '系统管理员', 2, 1);

INSERT INTO `User` (`Username`, `Password`, `RealName`, `RoleType`, `Status`)
VALUES ('dataadmin', 'admin123', '数据输入员', 1, 1);
```

---

**准备好开始了吗？请告诉我您想从哪个步骤开始！** 🚀

**建议的开始步骤：**
1. ✅ 先验证开发环境（Java、Maven、Node.js、MySQL）
2. ✅ 创建后端项目（使用Spring Initializr）
3. ✅ 创建前端项目（使用Vite）
4. ✅ 初始化数据库（执行建表脚本）
5. ✅ 配置并启动项目验证

**我可以帮您：**
- 生成完整的项目初始化命令
- 创建配置文件
- 编写基础代码框架
- 解决环境配置问题

